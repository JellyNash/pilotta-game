module.exports = {
  // Use an empty config to avoid stylelint errors while the stylesheet
  // conventions are still being finalized. This prevents CI failures until the
  // codebase is updated to satisfy stricter rules.
  extends: [],
  rules: {},
};
